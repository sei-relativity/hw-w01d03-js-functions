Method 1 

Name: Length
Description: Returns the length of a string.
Example using it:
let ex = "Find the length of thos text";
let leng = ex.length;

Method 2 

Name: Search 
Description: Searches a string for a specified value and returns the position of the match.
Example using it:
let ex = "Find the locate of GA ";
let pos = ex.search("GA");

Method 3

Name: lastIndexOf()
Description: Returns the index of the last occurrence of a specified text in a string.
Example using it:
let ex = "Where is general assembly occur" ;
let pos = ex.lastIndexOf("general");